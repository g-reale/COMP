int dotprod(int a, int b, int u, int v){
    return a * u + b * v;
}

void main(void){
    LCDADDR = 255;
    LCDADDR = 'd'; LCDADDR = 'o'; LCDADDR = 't';
    DISPADDR = dotprod(SWICHADDR,SWICHADDR,SWICHADDR,SWICHADDR);
}