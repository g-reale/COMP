int gcd (int u, int v)
{       
	if (v == 0) return u ;
	else return gcd(v,u-u/v*v);
}

void main(void){	
	
	int x; int y;

	LCDADDR = 255;
	LCDADDR = 'g'; LCDADDR = 'c'; LCDADDR = 'd';

	x = SWICHADDR; y = SWICHADDR;
	DISPADDR = gcd(x,y);
}
